package sse.ngts.testrobot.app.ui;

import javax.swing.JPanel;

public class ApplConfirmDialog extends JPanel
{
    /*
     * private JButton okButton ;
     * private JButton cancleButton;
     * private final static int width = 300;
     * private final static int height = 200;
     * private JDialog dialog;
     * private boolean ok;
     * 
     * public ApplConfirmDialog(String content)
     * {
     * JPanel panel = new JPanel();
     * panel.add(new JLabel(content));
     * add(panel,BorderLayout.CENTER);
     * 
     * cancleButton = new JButton("取消");
     * okButton = new JButton("确认");
     * JPanel buttonPanel = new JPanel();
     * buttonPanel.add(cancleButton);
     * buttonPanel.add(okButton);
     * panel.add(buttonPanel);
     * }
     * 
     * public boolean showDailog(Component parent,String title)
     * {
     * ok = false;
     * Frame owner = null;
     * if(parent instanceof Frame)
     * owner = (Frame)parent;
     * else
     * owner = (Frame)SwingUtilities.getAncestorOfClass(Frame.class, parent);
     * if(dialog == null ||parent.getParent()!= owner)
     * {
     * dialog = new JDialog(owner,true);
     * dialog.add(this);
     * dialog.getRootPane().setDefaultButton(okButton);
     * dialog.pack();
     * 
     * }
     * dialog.setTitle(title);
     * dialog.setVisible(true);
     * return ok;
     * }
     * 
     * public JButton getOkButton() {
     * return okButton;
     * }
     * 
     * public JButton getCancleButton() {
     * return cancleButton;
     * }
     */

}
